<!-- 69754e9c-1167-4f1f-a756-3581695520a4 d6feb674-7baa-427a-975f-6385ba49933d -->
# API + LangChain4j Foundation

## To-dos

- [x] 1. Add LangChain4j deps + properties
- [x] 2. Configure Ollama properties
- [x] 3. Define chat API contracts
- [x] 4. Wire LangChain4j services
- [x] 5. Expose REST controller + basic tests
- [ ] 6. Observability with Langfuse
- [ ] 7. Conversation memory (Redis-backed)
- [ ] 8. Essential RAG flow
- [ ] 9. Frontend/live coding helper
- [ ] 10. Environment isolation & DX

## Steps

1. [x] **Add LangChain4j dependencies**
1.1 [x] Update `build.gradle` with `langchain4j-spring-boot-starter` and `langchain4j-ollama`.
1.2 [x] Keep actuator/web/validation already present.

2. [x] **Configure Ollama properties**
2.1 [x] Extend `src/main/resources/application.yaml` with an `ollama` section (base URL, model).
2.2 [x] Add toggles (e.g., `llm.provider=ollama`) so we can change models or endpoints if needed.

3. [x] **Define chat API contracts**
3.1 [x] Create DTOs in `com.breno.llm.llm_microservice_observability_demo.chat.dto` (`ChatRequest`, `ChatResponse`, `MessageDTO`) with bean validation annotations.
3.2 [x] Document fields to keep the REST surface self-explanatory for workshop attendees.

4. [x] **Wire LangChain4j services**
4.1 [x] Implement a config class (`ChatModelConfig`) that exposes an Ollama-backed `ChatLanguageModel` bean based on the property toggle.
4.2 [x] Add a service layer (`ChatService`) that wraps LangChain4j calls, handles system prompts, and captures basic metadata (tokens, latency placeholders for future observability steps).

5. [x] **Expose REST controller + basic tests**
5.1 [x] Build `ChatController` under `chat` package: POST `/api/chat` -> validate request -> delegate to `ChatService`.
5.2 [x] Add a focused slice test or integration test using `@SpringBootTest` + mocked model bean to ensure the endpoint wiring is correct now, easing future observability additions.

6. [ ] **Observability with Langfuse**
**6.0 [x] configure a self hosted langfuse through gradle/**
6.1 [x] Configure an OpenTelemetry Collector image that forwards traces/metrics to Langfuse.
6.2 [ ] Configure the Spring Boot app to emit OTLP traces to the collector.
6.3 [ ] Configure the collector to forward to your Langfuse deployment (cloud or self-host).
6.4 [ ] `LangfuseChatModelListener` + `LangfuseClient` capture prompts, responses, token/cost/latency and push them to `/api/public/ingestion`.
6.5 [ ] Properties under `langfuse.*` (`enabled`, `base-url`, keys, environment) toggle the integration.
6.6 [ ] `README.md` documents how to wire the app against a manually managed Langfuse instance and how to enable/disable the integration.

7. [ ] **Conversation memory (Redis-backed)**
7.1 [ ] Implement short-term memory using `MessageWindowChatMemory`, keyed by `conversationId`.
7.2 [ ] Persist the memory in Redis (local or managed instance) so multiple app instances share state.
7.3 [ ] Extend `/api/chat` to accept optional `memory` flags and add coverage.

8. [ ] **Essential RAG flow**
8.1 [ ] Load demo documents (e.g., financing FAQ) into a local vector store (LangChain4j `EmbeddingStore`).
8.2 [ ] Reuse the same Redis/Qdrant instance chosen for memory (managed or locally installed).
8.3 [ ] Add a service that retrieves top-k chunks and injects them into the prompt before calling the model.
8.4 [ ] Provide unit/integration tests covering retrieval.

9. [ ] **Frontend/live coding helper**
9.1 [ ] Build a minimalist web client (HTML/JS or React) that hits `/api/chat` so workshop attendees see end-to-end flow.
9.2 [ ] (Optional) Serve it via Spring MVC static resources or a separate Vite dev server.

10. [ ] **Environment isolation & DX**
10.1 [ ] Create `docker-compose.yml` with services: Spring app, Ollama, Langfuse (+ Postgres), Redis (memory/RAG), any vector DB.
10.2 [ ] Add `Dockerfile` (or multi-stage) for the Spring app and document how to build/run everything via `docker compose up`.
10.3 [ ] Update `README.md` with quick-start instructions, environment variables, and troubleshooting tips.
10.4 [ ] Ensure tests can run locally (Gradle) and in container.

### To-dos

- [x] Add LangChain4j deps + properties
- [x] Create chat DTOs and validation
- [x] Wire ChatLanguageModel + service
- [x] Add controller and basic tests